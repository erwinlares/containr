% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-dockerfile.R
\name{generate_dockerfile}
\alias{generate_dockerfile}
\title{Generate a reproducible Dockerfile for an R project}
\usage{
generate_dockerfile(
  r_version = "current",
  r_mode = "base",
  auto_syslibs = TRUE,
  install_syslibs = NULL,
  output = ".",
  data_file = NULL,
  code_file = NULL,
  misc_file = NULL,
  add_user = NULL,
  home_dir = "/home",
  expose_port = "8787",
  install_quarto = FALSE,
  quarto_version = "latest",
  os_version = NULL,
  comments = FALSE,
  verbose = FALSE,
  config = NULL
)
}
\arguments{
\item{r_version}{A character string specifying the R version to use, e.g.
\code{"4.3.0"}. Defaults to \code{"current"}, which resolves to the version of R
running in the current session.}

\item{r_mode}{A character string selecting the Rocker base image. Inspired
by the \href{https://rocker-project.org/}{Rocker Project}. One of \code{"base"} for
plain R, \code{"tidyverse"} for R with the tidyverse, \code{"rstudio"} for RStudio
Server, \code{"verse"} for tidyverse plus TeX Live and publishing-related
packages, \code{"shiny_server"} for serving Shiny apps, or \code{"rstudio_shiny"}
for RStudio Server with Shiny Server layered on top. Defaults to
\code{"base"}.}

\item{auto_syslibs}{Logical. If \code{TRUE} (the default), reads \code{renv.lock}
from the current working directory, queries the Posit Package Manager
sysreqs database via \code{remotes::system_requirements()}, and automatically
includes the system libraries required by all packages in the lock file.
Warns and continues without auto-detection if the lookup fails. Set to
\code{FALSE} to skip auto-detection entirely.}

\item{install_syslibs}{A character vector or \code{NULL}. Additional system
libraries to install beyond those auto-detected from \code{renv.lock}. Each
element should be a valid \code{apt} package name, e.g.
\code{c("libuv1-dev", "libwebp-dev")}. Defaults to \code{NULL}.}

\item{output}{A character string. Directory path where the \code{Dockerfile}
will be written. Defaults to \code{"."}, the current working directory --
the same directory \code{build_image()} treats as the build context by
default, so the two functions' defaults compose without either
argument having to be supplied. A \code{Dockerfile} written somewhere else
(\code{tempdir()}, say) would have to be moved into the build context before
\code{build_image()} could find it, since the build context is always
\code{getwd()}. \code{output} is created automatically, along with any missing
parent directories, if it does not already exist.}

\item{data_file}{A character vector or \code{NULL}. Path(s) to data file(s)
and/or directories to copy into the container -- a single path, a
vector of paths, or a directory (copied whole, with its contents) may
all be mixed freely in the same vector. The local directory structure
is preserved under \code{home_dir} for \code{"base"}, \code{"tidyverse"}, \code{"rstudio"},
and \code{"verse"} (e.g. with the default \code{home_dir = "/home"},
\code{"data-raw/sample.csv"} becomes \verb{/home/data-raw/sample.csv}, and a
directory \code{"data-raw/"} is copied to \verb{/home/data-raw/} in full), or
under \verb{/srv/shiny-server/} for \code{"shiny_server"} and \code{"rstudio_shiny"},
matching Shiny Server's own default app directory regardless of
\code{home_dir}. Every path must be inside the current working directory
(the build context). Defaults to \code{NULL}.}

\item{code_file}{A character vector or \code{NULL}. Path(s) to script file(s)
(e.g. \code{.R}, \code{.qmd}, \code{.rmd}) and/or directories to copy into the
container -- see \code{data_file} for vector and directory behavior. The
local directory structure is preserved under the mode's copy root
(\code{home_dir} for four of the six modes) -- see \code{data_file}. Every path
must be inside the current working directory. Defaults to \code{NULL}.}

\item{misc_file}{A character vector or \code{NULL}. Path(s) to miscellaneous
file(s) (e.g. images, shell scripts, or branding assets) and/or
directories to copy into the container -- see \code{data_file} for vector
and directory behavior. The local directory structure is preserved
under the mode's copy root (\code{home_dir} for four of the six modes) --
see \code{data_file}. Every path must be inside the current working
directory. Defaults to \code{NULL}.
If the project was scaffolded with \code{\link[toolero:init_project]{toolero::init_project()}} using
\code{branding = TRUE} or \code{branding = "uw-madison"}, the generated \code{.qmd}
will reference \code{assets/styles.css}, \code{assets/header.html}, and
\code{assets/footer.html} at render time. Those files must be present inside
the container or Quarto will error on render. Pass
\code{misc_file = "assets/"} to copy the entire branding folder in one step.
Additional files and directories can be combined freely in the same
vector, e.g. \code{misc_file = c("assets/", "extra-script.sh")}.}

\item{add_user}{A character string. Name of a Linux user to create inside
the container with sudo access. Defaults to \code{NULL}.}

\item{home_dir}{A character string. The working directory set inside the
container via \code{WORKDIR}. For \code{"base"}, \code{"tidyverse"}, \code{"rstudio"}, and
\code{"verse"}, this is also where \code{data_file}, \code{code_file}, and \code{misc_file}
are copied (C24) -- there is nowhere else a script running from
\code{WORKDIR} could resolve its relative paths against. \code{"shiny_server"}
and \code{"rstudio_shiny"} are the exception: their copy destination is
fixed at \verb{/srv/shiny-server} regardless of \code{home_dir} -- see
\code{data_file}. Defaults to \code{"/home"}.}

\item{expose_port}{A character string. Overrides the port exposed when
\code{r_mode} is \code{"rstudio"}. Defaults to \code{"8787"}. Ignored for every other
\code{r_mode} -- \code{"shiny_server"} and \code{"rstudio_shiny"} expose their own
fixed port(s) (\code{"3838"}, and \code{"8787"}/\code{"3838"} respectively), since a
single override value can't address more than one port.}

\item{install_quarto}{Logical. If \code{TRUE}, downloads and installs the Quarto
CLI inside the container. Defaults to \code{FALSE}. See \code{quarto_version} to
pin a specific release rather than always installing whatever is
currently latest.}

\item{quarto_version}{Character string. Either \code{"latest"} (the default) or
an explicit Quarto version, e.g. \code{"1.5.57"}. Ignored unless
\code{install_quarto = TRUE}. When \code{"latest"}, the actual version is resolved
at generation time via the Quarto releases API and recorded in the
generated \code{Dockerfile} as \verb{ENV QUARTO_VERSION=...}, so a later rebuild
from the same \code{Dockerfile} reproduces the same Quarto version rather
than whatever happens to be current at build time -- consistent with
how \code{r_version} and \code{renv.lock} are pinned elsewhere in the image. An
explicit version is validated against the Quarto releases API and
errors if no matching release exists.}

\item{os_version}{A character string or \code{NULL}. The Ubuntu version to
query against when looking up system requirements for \code{auto_syslibs}
(C14). When \code{NULL} (the default), it is derived from the resolved
\code{r_version} via the Rocker Project's own R-version-to-Ubuntu-release
mapping -- see \code{.resolve_os_version()} -- rather than left at a single
hardcoded value that only matched the Ubuntu release actually backing
some R versions and not others. Supplying a value overrides the
derivation entirely, for a project that needs to query against a
different Ubuntu release than the one its \code{r_version} would normally
resolve to. Ignored when \code{auto_syslibs = FALSE}, since no sysreqs
lookup happens in that case.}

\item{comments}{Logical. If \code{TRUE}, annotates each Dockerfile instruction
with an explanatory comment. Useful for learning or sharing. Defaults to
\code{FALSE}. Note (C15): this is the one \code{comments} argument in the family
that writes into a generated file rather than printing to the console
-- \code{\link[=build_image]{build_image()}} and \code{\link[=push_image]{push_image()}} in this same package, and every
\code{comments} argument in \code{submitr}, use it to print explanatory guidance
to the console instead. Keep that distinction in mind when moving
between these functions.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages as each section
of the Dockerfile is written. Defaults to \code{FALSE}.}

\item{config}{A character string. Path to a \verb{_toolero.yml} project
manifest, such as the one \code{\link[toolero:init_project]{toolero::init_project()}} writes. When
supplied, fills in \code{data_file}, \code{code_file}, and \code{misc_file} from the
manifest's declared \verb{folders:} -- but only an argument left at its own
\code{NULL} default. An argument you do supply always wins; \code{config} never
overrides an explicit call. \code{code_file} is derived from the folder
named by the manifest's \code{script_dir} convention (\code{"R"} by default) when
that folder is present; \code{misc_file} is derived from \code{"assets"} when
present, the branding folder \code{init_project(branding = ...)} creates;
\code{data_file} is derived from \code{"data-raw"} when present, the folder this
family's own documentation uses as the canonical home for input data --
there is no dedicated manifest key naming it, so this one is
\code{containr}'s own convention rather than something the file states
explicitly. Reading \verb{_toolero.yml} is not a dependency on \code{toolero}: the
schema is the contract, and a manifest written by hand is as valid an
input as one \code{init_project()} created. In \code{verbose} mode, reports which
of \code{data_file}, \code{code_file}, and \code{misc_file} came from \code{config} rather
than from the call, since a generated \code{Dockerfile} whose \code{COPY} lines
came from somewhere invisible to the caller undermines the
reproducibility this package exists to support. A \code{schema_version} the
file does not declare is treated as schema \code{1}; any other declared
value produces a warning, not an abort, and the file is still read on a
best-effort basis either way. When supplied, \code{config} also adds a
\verb{RUN mkdir -p} instruction, right after \code{WORKDIR}, creating every
folder the manifest's \verb{folders:} declares under \code{home_dir} (C07) --
so a script's first write into one of them (via
\code{toolero::save_output()}, or a bare \code{ggsave()}/\code{write.csv()} call)
does not fail the way it would on a fresh checkout with no \code{config}
supplied. This is unconditional on which folders those are; a folder
this version of \code{containr} has no other special meaning for is still
created. Defaults to \code{NULL}, so nothing about this argument changes
the behavior of a call that does not use it.}
}
\value{
Called for its side effects. Writes a \code{Dockerfile} to \code{output}.
Returns \code{invisible(NULL)}.
}
\description{
\code{generate_dockerfile()} inspects an R project's dependencies via an \code{renv}
lockfile and writes a ready-to-use \code{Dockerfile} to the specified output
directory. It supports multiple Rocker base images, automatic system
library detection, Quarto installation, file copying, user creation, and
inline documentation comments.
}
\section{Prerequisites}{

\code{generate_dockerfile()} requires an \code{renv.lock} file in the current working
directory. Create one with \code{renv::snapshot()} before calling this function.
If the lock file is out of sync with your project library, a warning is
issued -- run \code{renv::snapshot()} to update it before building the image.

If the project uses Quarto with branding assets (i.e. \code{\link[toolero:create_qmd]{toolero::create_qmd()}}
was called with \code{use_style = TRUE}), the \verb{assets/} folder must be copied
into the container alongside the \code{.qmd} file or Quarto will be unable to
resolve the CSS and HTML includes at render time. The simplest way to
ensure this is to pass \code{misc_file = "assets/"} -- or
\code{misc_file = c("assets/", other_files)} if additional files are needed --
when calling \code{generate_dockerfile()}. No code change is required;
\code{misc_file} already accepts directories and copies them whole.
}

\examples{
\dontrun{
# Requires renv.lock in the current working directory.
# Run renv::snapshot() first if you don't have one.

# Generate a minimal Dockerfile using a pinned R version. output defaults
# to ".", so this writes to the current working directory -- the same
# place build_image() looks by default.
generate_dockerfile(r_version = "4.4.0")

# Pin a specific R version with the tidyverse image
generate_dockerfile(r_version = "4.3.0", r_mode = "tidyverse")

# Add extra system libraries on top of auto-detected ones
generate_dockerfile(
  r_version       = "4.4.0",
  install_syslibs = c("libuv1-dev", "libwebp-dev"),
  output          = "."
)

# Include a data file -- directory structure is preserved in the container
generate_dockerfile(
  r_version = "4.3.0",
  data_file = "data-raw/penguins.csv",
  code_file = "analysis.R",
  comments  = TRUE,
  output    = "."
)

# Multiple scripts and a whole assets folder -- pass assets/ via misc_file
# so that branding files (styles.css, header.html, footer.html) are present
# inside the container when Quarto renders the .qmd
generate_dockerfile(
  r_version = "4.3.0",
  code_file = c("R/prepare.R", "R/model.R"),
  misc_file = "assets/",
  output    = "."
)

# Serve a Shiny app -- files land under /srv/shiny-server/ automatically
generate_dockerfile(
  r_version = "4.3.0",
  r_mode    = "shiny_server",
  code_file = "app.R",
  output    = "."
)

# RStudio Server plus Shiny Server in the same image
generate_dockerfile(
  r_version = "4.3.0",
  r_mode    = "rstudio_shiny",
  code_file = "app.R",
  output    = "."
)

# Install Quarto, pinned to a specific release rather than whatever is
# currently latest -- the resolved version is recorded as
# ENV QUARTO_VERSION in the generated Dockerfile either way
generate_dockerfile(
  r_version      = "4.3.0",
  install_quarto = TRUE,
  quarto_version = "1.5.57",
  output         = "."
)

# Fill in data_file, code_file, and misc_file from a toolero project
# manifest instead of retyping paths the project already declares
generate_dockerfile(
  r_version = "4.3.0",
  config    = "_toolero.yml",
  output    = "."
)
}
}
