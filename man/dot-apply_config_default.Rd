% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-dockerfile.R
\name{.apply_config_default}
\alias{.apply_config_default}
\title{Apply one config-derived file argument default}
\usage{
.apply_config_default(current, derived, arg_name, config, verbose)
}
\arguments{
\item{current}{The argument's current value (from the call).}

\item{derived}{The value \code{\link[=.resolve_config_file_args]{.resolve_config_file_args()}} derived for it,
or \code{NULL}.}

\item{arg_name}{Character. The argument's name, for the \code{verbose} message.}

\item{config}{Character. Path to the config file, for the \code{verbose}
message.}

\item{verbose}{Logical. Whether to report the substitution.}
}
\value{
\code{current}, or \code{derived} when it applies.
}
\description{
Internal helper used by \code{\link[=generate_dockerfile]{generate_dockerfile()}}. Returns \code{current}
unchanged whenever the caller already supplied it, or whenever \code{config}
had no opinion about this argument. Only when both are true -- the
caller left it \code{NULL} and \code{config} derived a value -- does \code{derived}
take effect, and, in \code{verbose} mode, get reported as having come from
\code{config} rather than from the call.
}
\keyword{internal}
