% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/container-helpers.R
\name{.docker_config_has_auth}
\alias{.docker_config_has_auth}
\title{Check ~/.docker/config.json for a cached auth entry for a registry}
\usage{
.docker_config_has_auth(
  registry,
  config_path = path.expand("~/.docker/config.json")
)
}
\arguments{
\item{registry}{A character string naming the registry hostname.}

\item{config_path}{A character string. Path to the Docker config file.
Defaults to \verb{~/.docker/config.json}, the real location -- parameterized
so tests can point this at a temp file with real content instead of
mocking \code{jsonlite::fromJSON()} across a package boundary.}
}
\value{
Logical. \code{TRUE} if \code{registry} appears as a key under \code{auths} in
the Docker config file, \code{FALSE} if the file is missing, unreadable,
malformed, or simply has no entry for \code{registry}.
}
\description{
Check ~/.docker/config.json for a cached auth entry for a registry
}
\keyword{internal}
