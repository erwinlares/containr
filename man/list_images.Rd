% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list-images.R
\name{list_images}
\alias{list_images}
\title{List locally available container images}
\usage{
list_images(tool = NULL, verbose = FALSE)
}
\arguments{
\item{tool}{A character string or \code{NULL}. The container tool to use. One
of \code{"podman"} or \code{"docker"}. If \code{NULL} (the default), the function
auto-detects which tool is available, preferring \code{podman}.}

\item{verbose}{Logical. If \code{TRUE}, prints a progress message before
querying the local image store. Defaults to \code{FALSE}.}
}
\value{
A data frame with five columns: \code{repository}, \code{tag}, \code{image_id},
\code{created}, and \code{size}. Rows where both \code{repository} and \code{tag} are
\verb{<none>} correspond to untagged images produced by \code{\link[=build_image]{build_image()}} when
no \code{tag} argument was supplied. The data frame is also printed to the
console. Returns an empty data frame if no images are found.
}
\description{
\code{list_images()} returns a data frame of container images currently stored
in the local image store, as reported by \verb{podman image ls} or
\verb{docker image ls}. This is useful for finding the image ID to pass to
\code{\link[=push_image]{push_image()}} after building an image with \code{\link[=build_image]{build_image()}}.
}
\section{Finding your image ID}{

After calling \code{\link[=build_image]{build_image()}}, run \code{list_images()} to find the image ID
of the image you just built. Untagged images appear with \verb{<none>} in the
\code{repository} and \code{tag} columns -- the \code{image_id} column contains the hash
you need to pass to \code{\link[=push_image]{push_image()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{imgs <- list_images()
push_image(
  image_id = imgs$image_id[1],
  netid    = "erwin.lares",
  project  = "container-registry"
)
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# List all local images
list_images()

# Capture the result for programmatic use
imgs <- list_images()
imgs$image_id[1]
}
}
