% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r-mode-registry.R
\name{.extract_r_version_prefix}
\alias{.extract_r_version_prefix}
\title{Extract and pad the leading numeric R version from a version string}
\usage{
.extract_r_version_prefix(x)
}
\arguments{
\item{x}{Character string. A resolved R version, as produced by
\code{generate_dockerfile()}'s \code{r_version}/\code{"current"} resolution step.}
}
\value{
A three-component version string (e.g. \code{"4.4.0"}), or
\code{NA_character_} if \code{x} has no leading numeric portion.
}
\description{
Pulls the leading \code{X}, \code{X.Y}, or \code{X.Y.Z} numeric prefix from a resolved
\code{r_version} string and pads it to three components, so it can be safely
compared with \code{package_version()}. Handles the shapes \code{resolved_version}
can take: a bare major version (\code{"4"}), CUDA/Ubuntu suffixes
(\code{"4.4.0-cuda12.2-ubuntu22.04"}), and the non-numeric tags \code{"latest"} and
\code{"devel"}, for which it returns \code{NA_character_} -- both always resolve to
the current rocker-versioned2 image lineage, so callers should treat \code{NA}
here as "no floor applies."
}
\keyword{internal}
