% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/container-helpers.R
\name{.is_logged_in}
\alias{.is_logged_in}
\title{Check if the user appears to be logged in to a registry}
\usage{
.is_logged_in(tool, registry)
}
\arguments{
\item{tool}{A character string naming the resolved container tool.}

\item{registry}{A character string naming the registry hostname.}
}
\value{
Logical. \code{TRUE} if a cached login appears to exist, \code{FALSE}
otherwise.
}
\description{
Podman has a native way to answer this: \verb{podman login --get-login <registry>} exits 0 if a cached login exists for that registry and
non-zero otherwise. Docker has no equivalent flag -- \verb{docker login --get-login} is not a real Docker CLI option, and running it always
exits with a usage error (typically 125) regardless of whether the user
is actually logged in. Using it for both tools (the previous behavior)
meant Docker users always failed this check, logged in or not.
}
\details{
For Docker (and, permissively, any other tool -- \code{tool_preference} isn't
validated against a fixed list, see \code{.resolve_tool()}), this instead
inspects \verb{~/.docker/config.json} directly for an \code{auths} entry matching
\code{registry}. Docker itself has no query subcommand for "am I logged in
to X", so reading its own config file is the standard way tooling
(including most CI actions) checks this. Note that \verb{docker login} adds
a per-registry entry under \code{auths} even when a credential helper
(\code{credHelpers}) is configured for that registry, so this check works
correctly in that case too.

This is a best-effort local check, not a guarantee -- it confirms a
cached credential \emph{exists}, not that it's still \emph{valid}. An expired
token still leaves an entry in the config file (or Podman's auth
store), so a check that passes here can still fail at push time with
\verb{unauthorized: authentication required}. It also can't detect a login
set up entirely through a global \code{credsStore} (as opposed to a
per-registry \code{credHelpers} entry), since Docker defers to the external
credential store for those without writing anything to \code{auths} --
a narrow, acknowledged gap rather than a silent one.
}
\keyword{internal}
